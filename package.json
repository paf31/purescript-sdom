{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "pulp build"
  },
  "devDependencies": {
    "pulp": "^12.3.0",
    "purescript-psa": "^0.5.0",
    "purescript": "^0.12.0",
    "rimraf": "^2.5.4"
  }
}
